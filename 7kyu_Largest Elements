********* PROBLEM **********

Write a program that outputs the top n elements from a list.

Example:

largest(2, [7,6,5,4,3,2,1])
# => [6,7]

********* SOLUTION **********

def largest(n, xs):
    res = sorted(xs, reverse=True)
    ans = res[0:n]
    return ans[::-1]
